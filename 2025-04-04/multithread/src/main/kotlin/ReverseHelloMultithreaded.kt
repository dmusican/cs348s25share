/**
 * Write a program that creates a thread (let's call it Thread 1). Thread 1
 * creates another thread (Thread 2); Thread 2 creates Thread 3; and so on, up
 * to Thread 50. Each thread should print "Hello from Thread <num>!", but you
 * should structure your program such that the threads print their greetings in
 * reverse order.
 */
fun doReverseHello() {
}
